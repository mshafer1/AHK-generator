  
language: python
python:
  - 3.7
notifications:
  email: false
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
install:
  - pip install -r "test_requirements.txt"
script: pytest # run the tests

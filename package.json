{
    "scripts": {
        "coveralls": "cat ./coverage/lcov.info | coveralls",
        "test": "jest --coverage",
        "jupdate": "jest -u"
    },
    "devDependencies": {
        "coveralls": "^3.1.0",
        "jest": "^26.4.0",
        "jquery": "^3.5.1",
        "lzutf8": "^0.5.6"
    },
    "dependencies": {}
}

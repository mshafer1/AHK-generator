{
    "scripts": {
        "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls",
        "test": "jest"
    },
    "devDependencies": {
        "coveralls": "^3.1.0",
        "jest": "^26.0.1"
    }
}